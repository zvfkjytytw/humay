http_config:
    host: localhost
    port: 8080
    read_timeout: 5
    write_timeout: 10
    idle_timeout: 20
saver_config:
    interval: 300
    storage_file: /tmp/metrics-db.json
    restore: false
database_dsn: ""
# pg_config:
#     host: localhost
#     port: 5432
#     database: humay
#     user: humay
#     password: PGPASSWORD
